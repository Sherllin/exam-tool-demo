# OpenAI-compatible Chat Completions endpoint. Leave any value empty to use
# the explicit local demo fallback instead of calling a remote model.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=
LLM_TIMEOUT_SECONDS=12
